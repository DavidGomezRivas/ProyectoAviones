package ProyectoAviones;

public class Main {
    public static void main(String[] args) {
        // Crea una instancia de la clase Inicial.
        Inicial inicial = new Inicial();
        // Hace visible la instancia de Inicial, mostrando la ventana de la interfaz gráfica.
        inicial.setVisible(true);
    }
}